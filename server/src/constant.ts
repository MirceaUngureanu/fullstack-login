export const __prod__ = process.env.NODE_ENV === 'production'
export const COOKIE_NAME = "qid"

// if nodemailer test user and pass expire use the createTestAccount function in "sendEmail.ts" to generate a new one
export const TEST_NODEMAILER_USE = ***REMOVED***
export const TEST_NODEMAILER_PASS = ***REMOVED***